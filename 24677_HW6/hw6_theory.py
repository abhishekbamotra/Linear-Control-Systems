# You dont need to run this script.
# It will be executed by the GradeScope Autograder
# Do not input anything else other than the instructions given in comments, remember that python is tab and caps sensitive.
# In the place of *, input your answer.
# Round your answers to 2 decimals. eg: 0.114 -> 0.11, 0.115 -> 0.12. eg: 0.100 -> 0.10

import unittest # Import the gradescope autograder library. No actions are needed here.
import numpy as np # Import numpy lirary. No actions are needed here.
import scipy as spy # No actions are needed here as well.
import math


class AnswersHw6():
	def __init__(self):
		self.exercise_1()
		self.exercise_2()
		self.exercise_3()
		self.exercise_4()
		self.exercise_5()
        
	def exercise_1(self):
		# Round your answers to 2 decimals. eg: 0.114 -> 0.11, 0.115 -> 0.12 
		self.answer1_P=np.array(*)	# controllability matrix 
		self.answer1_controllable = *;	# True: if controllable, False: if not
		self.answer1_Q=np.array(*)	#Observability matrix
		self.answer1_observable = *;	# True: if observable, False: if not
        
	def exercise_2(self):
		# Round your answers to 2 decimals. eg: 0.114 -> 0.11, 0.115 -> 0.12 
		self.answer2_P=np.array(*)	# controllability matrix 
		self.answer2_controllable = *;	# True: if controllable, False: if not
		self.answer2_Q=np.array(*)	#Observability matrix
		self.answer2_observable = *;	# True: if observable, False: if not
        
	def exercise_3(self):
		# Round your answers to 2 decimals. eg: 0.114 -> 0.11, 0.115 -> 0.12 
		self.answer3_controllable = *;	# True: if controllable, False: if not
		self.answer3_observable = *;	# True: if controllable, False: if not

	def exercise_4(self):
		# Round your answers to 2 decimals. eg: 0.114 -> 0.11, 0.115 -> 0.12 
		self.answer4_controllable = *;	# True: if controllable, False: if not
		self.answer4_observable = *;		# True: if controllable, False: if not
        
	def exercise_5(self):
		# Round your answers to 2 decimals. eg: 0.114 -> 0.11, 0.115 -> 0.12 
		self.answer5_eigenvalues = [*];  # Enter the eigen value(s) in the list seperated by commas
        

        
